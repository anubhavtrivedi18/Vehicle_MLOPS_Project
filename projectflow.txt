1. Create project template by exceuting template.py file
2. Write the code on setup.py and pyproject.toml file to import local packages
    >> Find more about "setup.py and pyproject.toml file to import local packages
3. Create a virtual env, acivate it and instal  the requirements from requirements.txt
      conda create -n proj1 python=3.10 -y
      conda activate proj1
      add required modules to requirements.txt
      Do "pip install -r requirements.txt"

4.  Do a pp list on terminal to make sure you have local packages installed.